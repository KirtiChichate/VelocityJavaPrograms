package generalization;

public interface GenSuperClass_ 
{
	int value=25;
//In this only we declare method and this class is universal for classes so its methodName is common for all other subclasses
	void Addition();
	void Employee_details();
	void Multiplication();
}
